services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: oaflang-dev:latest
    working_dir: /workspace
    volumes:
      - ./:/workspace
    stdin_open: true
    tty: true
    command: bash
